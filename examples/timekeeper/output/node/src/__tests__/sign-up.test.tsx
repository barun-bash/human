// Generated by Human compiler — do not edit

import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import { BrowserRouter } from 'react-router-dom';
import { SignUp } from '../pages/sign-up';

jest.mock('../api/client');

const renderPage = () => render(
  <BrowserRouter>
    <SignUp />
  </BrowserRouter>
);

describe('SignUp page', () => {
  it('should render without crashing', () => {
    renderPage();
  });

  it('should display the app name and tagline Create your account', () => {
    renderPage();
    expect(screen.getByText(/the app name and tagline Create your account/i)).toBeInTheDocument();
  });

  it('should handle clicking Sign Up does CreateAccount', () => {
    renderPage();
    const button = screen.getByText(/Sign Up does CreateAccount/i);
    fireEvent.click(button);
  });

  it('should handle clicking Sign In navigates to SignIn', () => {
    renderPage();
    const button = screen.getByText(/Sign In navigates to SignIn/i);
    fireEvent.click(button);
  });

  it('should show loading state', () => {
    renderPage();
    expect(screen.getByText(/loading/i)).toBeInTheDocument();
  });

});
