# Generated by Human compiler — do not edit

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: timekeeper
    ports:
      - "5432:5432"
    volumes:
      - timekeeper-data:/var/lib/postgresql/data

  backend:
    build:
      context: ./go
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/timekeeper?sslmode=disable
      JWT_SECRET: ${JWT_SECRET}
      PORT: "8080"

  frontend:
    build:
      context: ./react
      args:
        VITE_API_URL: http://localhost:8080
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  timekeeper-data:
