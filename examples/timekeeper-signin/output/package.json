{
  "name": "timekeeper",
  "version": "0.1.0",
  "private": true,
  "description": "TimeKeeper — generated by Human compiler",
  "workspaces": [
    "node",
    "react"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=node\" \"npm run dev --workspace=react\"",
    "start": "concurrently \"npm run start --workspace=node\" \"npm run start --workspace=react\"",
    "build": "npm run build --workspace=node && npm run build --workspace=react",
    "test": "npm run test --workspace=node",
    "db:migrate": "cd node && npx prisma migrate deploy",
    "db:seed": "cd node && npx prisma db seed",
    "db:studio": "cd node && npx prisma studio",
    "docker:dev": "docker compose up --build",
    "docker:start": "docker compose up -d",
    "docker:stop": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
