# Generated by Human compiler — do not edit

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: timekeeper
    ports:
      - "7433:5432"
    volumes:
      - timekeeper-data:/var/lib/postgresql/data

  backend:
    build:
      context: ./node
    restart: unless-stopped
    ports:
      - "7420:7420"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/timekeeper?schema=public
      JWT_SECRET: ${JWT_SECRET}
      PORT: "7420"

  frontend:
    build:
      context: ./react
      args:
        VITE_API_URL: http://localhost:7420
    restart: unless-stopped
    ports:
      - "7320:80"
    depends_on:
      - backend

volumes:
  timekeeper-data:
